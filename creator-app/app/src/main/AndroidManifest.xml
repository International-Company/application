<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- بطاقة الإرشاد فوق TikTok. لا تقرأ الشاشة ولا تلتقط اللمس. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--
      لا يطلب التطبيق إذن قراءة الرسائل إطلاقًا: رمز التحقق يصل عبر
      SMS Retriever الذي يسلّم رسالة واحدة موجّهة لهذا التطبيق وحده.
    -->

    <!-- الاستعلام عن TikTok فقط لفتحه والتحقق من توقيعه -->
    <queries>
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.trill" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Mobde3">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- تعبئة بيانات الاستلام داخل TikTok وحده -->
        <service
            android:name=".autofill.Mobde3AutofillService"
            android:exported="true"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE">
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service" />
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
        </service>

        <!-- التقاط إشعارات TikTok الموثّقة بتوقيعها -->
        <service
            android:name=".notifications.TikTokNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".overlay.GuideOverlayService"
            android:exported="false" />

        <!-- إشعارات المبدع عن حالة طلبه -->
        <service
            android:name=".notifications.CreatorMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
